.article-form	
  - form_for([:admin, @events]) do |f|
    = f.error_messages
    %p.label
      = f.label :title
      %br
      = f.text_field :title, :size => 45
    %p.label
      = f.label :body
      %br
      = f.text_area :body
    %p
      = f.submit 'Save'
