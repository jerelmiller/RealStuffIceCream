- content_for :title do
  Admin: New Flavor
= render :partial => 'admin/partials/header'

.main
  .edit-title
    %h1 New Flavor
  .edit-links
    %ul
      %li= link_to image_tag ('/images/icon_small_larrow.png', :border => 0) + ' back to flavors', admin_flavors_path
  = render :partial => 'admin/flavors/form'
